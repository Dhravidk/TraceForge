#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ -x "$ROOT/.venv/bin/jac" ]]; then
  exec "$ROOT/.venv/bin/jac" run "$ROOT/traceforge/cli.jac" "$@"
fi

exec jac run "$ROOT/traceforge/cli.jac" "$@"
