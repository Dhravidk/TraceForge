# Tests

Use this directory for lightweight acceptance checks and validation notes.

Intended contents:
- parser smoke tests,
- feature extraction checks,
- similarity sanity checks,
- and short manual test instructions for the demo path.

Keep tests small and deterministic so they can run quickly during hackathon iteration.

